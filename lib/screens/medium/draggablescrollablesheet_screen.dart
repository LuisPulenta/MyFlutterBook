import 'package:flutter/material.dart';

class DraggablescrollablesheetScreen extends StatelessWidget {
  const DraggablescrollablesheetScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DraggablescrollablesheetScreen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('DraggablescrollablesheetScreen'),
      ),
    );
  }
}
