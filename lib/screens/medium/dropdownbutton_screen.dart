import 'package:flutter/material.dart';

class DropdownbuttonScreen extends StatelessWidget {
  const DropdownbuttonScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DropdownbuttonScreen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('DropdownbuttonScreen'),
      ),
    );
  }
}
