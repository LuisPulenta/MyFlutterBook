import 'package:flutter/material.dart';

class AnimatedswitcherScreen extends StatelessWidget {
  const AnimatedswitcherScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AnimatedswitcherScreen '),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('AnimatedswitcherScreen '),
      ),
    );
  }
}
