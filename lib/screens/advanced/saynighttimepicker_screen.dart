import 'package:flutter/material.dart';

class SaynighttimepickerScreen extends StatelessWidget {
  const SaynighttimepickerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SaynighttimepickerScreen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('SaynighttimepickerScreen'),
      ),
    );
  }
}
