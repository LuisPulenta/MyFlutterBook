import 'package:flutter/material.dart';

class AnimatedroundingScreen extends StatelessWidget {
  const AnimatedroundingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AnimatedroundingScreen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('AnimatedroundingScreen'),
      ),
    );
  }
}
