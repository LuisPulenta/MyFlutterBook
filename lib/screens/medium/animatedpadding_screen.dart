import 'package:flutter/material.dart';

class AnimatedpaddingScreen extends StatelessWidget {
  const AnimatedpaddingScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AnimatedpaddingScreen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('AnimatedpaddingScreen'),
      ),
    );
  }
}
