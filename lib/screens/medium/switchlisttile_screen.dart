import 'package:flutter/material.dart';

class SwitchlisttileScreen extends StatelessWidget {
  const SwitchlisttileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('SwitchlisttileScreen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('SwitchlisttileScreen'),
      ),
    );
  }
}
