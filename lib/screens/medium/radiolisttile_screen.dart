import 'package:flutter/material.dart';

class RadiolisttileScreen extends StatelessWidget {
  const RadiolisttileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('RadiolisttileScreen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('RadiolisttileScreen'),
      ),
    );
  }
}
