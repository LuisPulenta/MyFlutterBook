import 'package:flutter/material.dart';

class FluttercircularmenuScreen extends StatelessWidget {
  const FluttercircularmenuScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('FluttercircularmenuScreen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('FluttercircularmenuScreen'),
      ),
    );
  }
}
