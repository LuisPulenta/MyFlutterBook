import 'package:flutter/material.dart';

class AnimationiconsScreen extends StatelessWidget {
  const AnimationiconsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AnimationiconsScreen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('AnimationiconsScreen'),
      ),
    );
  }
}
