import 'package:flutter/material.dart';

class IndexedstackScreen extends StatelessWidget {
  const IndexedstackScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('IndexedstackScreen'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('IndexedstackScreen'),
      ),
    );
  }
}
